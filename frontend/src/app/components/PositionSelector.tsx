// Re-export from shared (single source of truth, also used by desktop).
// The shared PositionSelector.tsx is copied to src/shared/ via the prebuild script.
export { default } from "../../shared/PositionSelector";
