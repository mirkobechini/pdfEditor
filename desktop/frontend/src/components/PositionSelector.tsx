// Re-exported for backward compatibility with existing imports.
// Canonical implementation now lives in shared/src/PositionSelector.tsx —
// web shares it too instead of duplicating (see frontend's SignDialog.tsx).
export { default } from "../shared/PositionSelector";
