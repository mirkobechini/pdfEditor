{
  "name": "pdfeditor-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "E2E tests for PdfEditor (Playwright)",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "cross-env": "^10.1.0"
  }
}
